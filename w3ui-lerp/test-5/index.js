"use strict";
(function(){
  var x, state, nodeMap, graph, onUpdate, tween;
  x = window.performance.now();
  for (;;) {
    if (Math.abs(window.performance.now() - x) > 60*1000) {
      break;
    }
  }
  return;
  })();
